@model CHomeViewModel
<div id="content">
    @await Html.PartialAsync("_Sketches", Model.CurrentPosts)
    @if (Model.AllFilteredPostsNum > Model.PostsPerPage)
    {
        @await Html.PartialAsync("_Pagination", Model)
    }
</div>